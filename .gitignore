# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (do not commit secrets)
.env
.env*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db

# Local design handoff (not source-of-truth; reference only)
/_design-handoff/

# Local CC1 launch prompt at the repo root — copy/paste content, never shared
# (CC2 launch prompt is now repo-tracked at docs/Tests/CC2_START_PROMPT.md)
/CC1_START_PROMPT.md

# Local junk drops at repo root — debug screenshots, scratch notes,
# audio test files. Real documentation belongs in /docs, never the root.
/TODO.txt
/Contour*.png
/a[0-9]*.png
/b[0-9]*.png
/c[0-9]*.png
/POstenaustausch*
/tts-preview-test.mp3

# Local archive drops — never commit (backups/exports, can exceed GitHub limits)
*.zip

# Word / Excel / PowerPoint temp lock files (created next to the open file)
~$*

# Large local demo recordings — keep on disk for reference, never commit
docs/Tests/Demoszene/Recording*.mp4
docs/vibegrid_export_*.mp4

# Local Sound Library MP3 source files (~100 MB) — uploaded to R2 by
# tools/upload-vibegrid-mp3s.mjs, never committed.
/tools/VibeGrid_MP3/
