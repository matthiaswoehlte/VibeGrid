# QA Report — Plan 5.7 + 5.7-R
Datum: 2026-05-20
Commit: `df0c4f81060f2beccab40ed67e93550f4c177455` (`df0c4f8`)

## Verification Gate
- npm test:      **358** Tests, **0** failing  ✅ (61 Files, 6.69 s, exakt die erwartete Baseline)
- typecheck:     ✅ (`tsc --noEmit` clean, keine Errors/Warnings)
- lint:          ✅ (`✔ No ESLint warnings or errors`)
- build:         ✅ (Compiled successfully, Studio-Page `/` = **35.4 kB / 131 kB First Load JS**, alle 4 API-Routes `0 B / 0 B` Dynamic)

## Git Log
- **109 Commits** ahead of `origin/main` insgesamt
- Davon **Plan-5.7-Scope** (10 Commits): `1e51042` (snap helper), `35142a8` (store batch + UI state), `dd989fa` (snap picker), `8b78a08` (modifier drag), `265a8f7` (dbl-click overlay), `4116bd1` (read-only lane + modal split), `c3d6d14` (long-press delete), `1b4ec99` (plan-doc 5.7), `bc0f9c5` (plan-doc 5.7-R), `df0c4f8` (rename `expanded…` → `automationEditor…`)
- Commit-Message-Format `type(scope): description`: **ja** für alle Plan-5.7-Commits (`feat(timeline):`, `feat(store):`, `feat(automation):`, `fix(automation):`, `docs(plan):`)

**Diff-Umfang (gesamt ahead):** 148 Dateien, +19794 / −267 Zeilen. Plan-5.7-spezifisch ergänzt:
- `lib/automation/snap.ts` (neu, 17 LOC)
- `components/Workspace/Timeline/AutomationPoint.tsx` (+237 LOC, vollständig neu strukturiert mit Long-press + Modifier-Drag)
- `components/Workspace/Timeline/AutomationEditorModal.tsx` (neu, 161 LOC)
- `components/Workspace/Timeline/AutomationCurveEditor.tsx` (neu, 196 LOC)
- `components/Workspace/Timeline/AutomationPointEditOverlay.tsx` (neu, 116 LOC)
- `components/Workspace/Timeline/AutomationLane.tsx` (read-only refactor, 97 LOC)
- `lib/store/index.ts` (UI state additions, +58 / −16)
- `lib/store/timeline-slice.ts` (`updateParamPoints` batch action, +207 / −7)
- `tests/unit/automation/snap.test.ts` (neu, 40 LOC)
- `tests/unit/components/Timeline/AutomationPoint-modifiers.test.tsx` (neu, 135 LOC)
- `tests/unit/components/Timeline/AutomationLane.test.tsx` (read-only rewrite, 117 LOC)

## Code Review Findings

### `components/Workspace/Timeline/AutomationPoint.tsx`
- [x] ✅ `setPointerCapture` vorhanden in `onPointerDown` — `:61` `target.setPointerCapture(pointerId)` mit try/catch für jsdom
- [x] ✅ Dual-Circle `<g>`: r=12 transparenter Hit-Area + r=6 sichtbarer Dot — `:225-234` (`r={12} fill="rgba(0,0,0,0)" pointerEvents="all"` + `r={6} fill="var(--a2)" pointerEvents="none"`)
- [x] ✅ `pointerEvents="none"` auf sichtbarem Dot — `:233`
- [x] ✅ Long-press Timer (`setTimeout 600 ms`) vorhanden — `:70-71` `const longPressMs = 600; const longPressTimer = setTimeout(...)`
- [x] ✅ Long-press Timer wird gecancelt wenn Pointer sich bewegt — `:69 let moved = false` Flag; `:115-118` `if (!moved && (Math.abs(dx) > 4 || Math.abs(dy) > 4)) { moved = true; clearTimeout(longPressTimer); }`. **⚠️ Kleine Abweichung:** Der Prompt nennt ">3 px", der Code verwendet ">4 px". Beide erfüllen den `didMove`-Flag-Pattern; Threshold-Differenz ist semantisch unkritisch (4 px ist sogar etwas robuster gegen Mikro-Tremor).
- [x] ✅ `window.removeEventListener` in cleanup vorhanden — `:74-78` (im Timer-Branch), `:171-176` (im up-Handler). Sowohl `target`-Listener als auch `window`-Fallback-Listener werden entfernt
- [x] ✅ Kein `grab` / `grabbing` Cursor — `:198-199` expliziter Kommentar "Default cursor everywhere — no grab/grabbing cursor styling in the automation area". `grep cursor-grab` im Component-File: 0 Treffer

### `lib/automation/snap.ts`
- [x] ✅ `AutomationSnap` Union exportiert: `'off' | '1' | '1/2' | '1/4' | '1/8' | '1/16'` — `:1` exakt diese 6 Werte
- [x] ✅ `snapBeat` gibt bei `'off'` den Input zurück (mit Clamp ≥ 0) — `:14` `if (unit === 'off') return Math.max(0, beat);`
- [x] ✅ Negative Inputs werden auf 0 geclampt — `:14` (off-Pfad) und `:16` `Math.max(0, Math.round(beat / step) * step)` (gesnappter Pfad)

### `lib/store/index.ts`
- [x] ✅ `automationSnap: 'off'` in der UI-Initial-State-Literal — `:22`
- [x] ✅ `automationSnap` steht NICHT in `partialize` — `:67` `ui: { zoom: state.ui.zoom }` (nur `zoom` aus UI), Doc-Kommentar `:62-66` listet ausdrücklich `selectedClipId`, `automationEditorClipId` UND `automationSnap` als transient
- [x] ✅ `automationEditorClipId` Kommentar erklärt neue Semantik UND Field wurde umbenannt — beides erfüllt: `:14-17` "automationEditorClipId was named expandedAutomationClipId in Plan 5.5/5.6 (inline-lane toggle). Plan 5.7-R repurposed the same field to drive the full-screen AutomationEditorModal; cleanup semantics on selectedClipId/removeClip are unchanged." Rename-Commit: `df0c4f8`

### `lib/store/timeline-slice.ts`
- [x] ✅ `updateParamPoints` Action vorhanden — `:167-187` `updateParamPoints: (clipId, key, updates) => { … }`
- [x] ✅ Early-exit bei leerem `updates`-Array — `:168` `if (updates.length === 0) return;` als allererste Anweisung

## Test Coverage Spot-Check

### `tests/unit/automation/snap.test.ts`
- [x] ✅ Test für `'off'` (pass-through) — `:5-7` "returns the input unchanged when snap is off"
- [x] ✅ Test für negativen Input (clamp zu 0) — `:31-34` "clamps negative inputs to 0" (deckt sowohl `'1/4'`-Pfad als auch `'off'`-Pfad ab)
- [x] ✅ Min. 1 Test für `'1/16'` — `:24-29` "'1/16' rounds to 0.0625 increments" mit zwei Assertions (1.05 → 1.0625, 1.03 → 1.0)

### `tests/unit/components/Timeline/AutomationPoint-modifiers.test.tsx`
- [x] ✅ Ctrl-Test (beat locked) — `:94-101` "ctrl — beat locked, only value moves" (beat bleibt 2 trotz dx=40)
- [x] ✅ Shift-Test (trailing points follow) — `:103-110` "shift — trailing points follow by same delta" (alle nachgelagerten Punkte +1 Beat)
- [x] ⚠️ Long-press-Test mit `vi.useFakeTimers()` — **nicht in diesem File**. Die Long-press- und Long-press-Cancel-Tests liegen in `tests/unit/components/Timeline/AutomationPoint.test.tsx` (separates File, im git-stat sichtbar mit 168 LOC). Habe es nachgelesen und bestätige: `vi.useFakeTimers()` + Long-press-Delete + Long-press-Cancel-bei-Movement sind dort abgedeckt. Datei-Splitting (`-modifiers.test.tsx` vs. das Base-File) statt einer Sammeldatei — Funktional vollständig.
- [x] ✅ Test dass Long-press gecancelt wird wenn Pointer sich bewegt — in `AutomationPoint.test.tsx` (siehe oben), nicht im `-modifiers`-File

### `tests/unit/components/Timeline/AutomationLane.test.tsx`
- [x] ✅ Bestehende drag/click-Pfade abgedeckt ODER als "read-only, nicht zutreffend" markiert — der Describe-Block heißt explizit `'AutomationLane (read-only preview)'` (`:43`) und enthält den Test "does NOT render the snap, interpolation or close controls" (`:67-72`) und "renders non-interactive points (no point events attached)" (`:82-88`)
- [x] ⚠️ Snap-Picker-Tests vorhanden — **nicht in `AutomationLane.test.tsx`** (die Lane ist jetzt read-only und hat keinen Picker mehr). Der Picker wohnt jetzt in der `AutomationEditorModal` bzw. `AutomationCurveEditor`. Diese Komponenten haben kein eigenes `.test.tsx`-File (nur `EditOverlay.test.tsx` und die `-modifiers`-Tests testen Snap-Anwendung indirekt). **Hinweis** — siehe "Offene Punkte".

## Playwright

- **5 passed / 0 failed / 0 skipped**
- Test-File: `tests/e2e/studio.spec.ts` (5 Tests aus dem Plan-5-QA-Lauf, weiterhin Smoke-only)
- Tests:
  - `page loads with title VibeGrid and canvas is visible` ✅ 1.2 s
  - `TopBar exposes a play control` ✅ 428 ms
  - `LeftPanel shows Media / FX / Layers tabs` ✅ 300 ms
  - `Inspector shows the empty-selection hint initially` ✅ 311 ms
  - `no uncaught JS errors during initial load` ✅ 2.4 s
- WebServer fiel auf Port 3004 zurück (3000-3003 belegt — unkritisch, Playwright fängt das ab)

**🟡 Coverage-Lücke:** Es gibt keinen Plan-5.7-spezifischen E2E-Test (Modal öffnen, Punkt verschieben, Snap aktivieren). Ist out-of-scope für diesen QA-Lauf (Prompt verlangte nur Smoke), aber ein Snap-Picker- + Modal-Open-Smoke wäre für Plan 6 sinnvoll.

## Offene Architekt-Punkte

- **Bug 1 (Long-press Drag-Cancel):** ✅ implementiert
  - `AutomationPoint.tsx:69` `let moved = false` Flag
  - `AutomationPoint.tsx:115-118` Bewegung > 4 px → `moved = true; clearTimeout(longPressTimer)`
  - `AutomationPoint.tsx:71-86` Timer-Fire prüft zusätzlich `if (moved) return;` als Doppelsicherung
  - Anmerkung: 4 px statt der im Prompt erwähnten 3 px Threshold (siehe Code-Review oben).
- **Bug 2 (`expandedAutomationClipId` Semantik):** ✅ dokumentiert UND umbenannt
  - Field umbenannt zu `automationEditorClipId` (commit `df0c4f8`, betrifft `lib/store/index.ts`, `lib/store/types.ts`, `lib/store/timeline-slice.ts` + alle Consumer)
  - Kommentar in `lib/store/index.ts:12-17` erklärt die Historie (Plan 5.5/5.6 → 5.7-R) und neue Semantik (Modal-Sichtbarkeit)
  - CLAUDE.md wurde **nicht** aktualisiert — die Datei referenziert keine spezifischen UI-State-Felder (nur Module-Map auf Verzeichnisebene), daher kein Update nötig. Architekt-Anforderung (a) ODER (b) ist durch Rename + Kommentar erfüllt.

## Offene Punkte (zusätzlich, kein Showstopper)

1. **Snap-Picker hat keinen dedizierten Component-Test** — der Picker lebt jetzt in `AutomationEditorModal` / `AutomationCurveEditor`, beide ohne eigenes `.test.tsx`-File. Snap-Verhalten wird indirekt durch den Modifier-Test (`-modifiers.test.tsx:126-134` "snap '1/4' rounds the active beat") abgedeckt, aber ein Test, der konkret das Picker-UI (`<select>` mit `'off' | '1' | '1/2' …`) rendert und auf `setAutomationSnap` synced, fehlt. **Empfehlung für Plan 6:** ergänzen, falls der Modal-Editor weitere Controls bekommt.

2. **Long-press-Threshold-Drift** — Prompt verlangt ">3 px", Code nutzt ">4 px". Funktional gleichwertig, aber wenn das eine harte Spec-Anforderung war, sollte entweder die Spec oder der Code angepasst werden. Empfehle: dokumentieren in der Spec (z. B. "movement threshold ≥ 3 px, current impl 4 px").

3. **Playwright-Webserver-Port-Drift** — Beim QA-Lauf waren Ports 3000-3003 belegt (Folge mehrerer offener Dev-Server). Playwright handelt das korrekt, aber für CI ist das Verhalten unkritisch (frische Workers). Watchlist für CI-Pipeline.

## Gesamturteil

✅ **Freigegeben für Plan 6.**

Begründung: Verification Gate komplett grün (358/358 Tests, Build, Typecheck, Lint), beide vom Architekten geforderten Fixes sind sauber implementiert (Bug 1 mit `didMove`-Flag + 600 ms Timer, Bug 2 sowohl mit Rename als auch Doc-Kommentar), Code-Review-Checkliste 12/12 erfüllt (mit einer kosmetischen Threshold-Abweichung 4 px statt 3 px), Test-Coverage-Spot-Check 7/8 ✅ + 2 organisatorische Hinweise (Long-press-Tests in separater Datei, Snap-Picker-UI ohne dediziertes File). Playwright-Smoke 5/5. Keine ❌-Findings.
