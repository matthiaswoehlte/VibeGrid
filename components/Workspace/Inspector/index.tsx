'use client';
// Stub — replaced by Task 20 (auto-form from paramSchema + PreloadIndicator).
export function Inspector() {
  return <div className="p-3 text-xs text-[var(--text-dim)]">Inspector (Task 20)</div>;
}
