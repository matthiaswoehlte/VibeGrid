'use client';
// Stub — replaced by Task 15 with upload + AutoPresetButton + drag-to-timeline.
export function MediaLibrary() {
  return <div className="text-xs text-[var(--text-dim)]">Media library — wired in Task 15.</div>;
}
